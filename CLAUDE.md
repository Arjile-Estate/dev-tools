# Dev-Tools Usage Guide for AI Assistants

## Project Information
- **Project Type**: Go
- **Project Directory**: .
- **Tool**: dev-tools (unified command runner)

## Available Project Commands

These commands are available for this Go project:


### `dev-tools advanced-services`
Run 1 commands (custom)

### `dev-tools build`
Run go build -o dev-tools . (custom)

### `dev-tools dev`
Run 1 commands (custom)

### `dev-tools dev-compose`
Run 1 commands (custom)

### `dev-tools dev-logs`
Run tail -n 50 activity.log (custom)

### `dev-tools integration-test`
Run 1 commands (custom)

### `dev-tools lint`
Run 2 commands (custom)

### `dev-tools logs`
Run tail -f activity.log (custom)

### `dev-tools test`
Run go test ./... -v (custom)

### `dev-tools test-background-daemon`
Run sleep 30 (custom)

### `dev-tools test-command-with-args`
Run 2 commands (custom)

### `dev-tools test-compose`
Run 1 commands (custom)

### `dev-tools test-daemon`
Run echo toot && sleep 10 (custom)

### `dev-tools test-daemon-with-directory`
Run echo 'Running in:' && pwd && sleep 5 (custom)

### `dev-tools test-directory`
Run pwd && ls -la (custom)

### `dev-tools test-failing`
Run echo This command will fail && exit 42 (custom)

### `dev-tools test-multicmd`
Run 2 commands (custom)

### `dev-tools test-realtime`
Run echo Starting... && sleep 1 && echo M... (custom)

### `dev-tools test:coverage`
Run go test -cover ./... (custom)


## Built-in Dev-Tools Commands

These management commands are always available:


### `dev-tools logs`
View recent activity logs

### `dev-tools status`
Show daemon process status

### `dev-tools cleanup-pids`
Clean up stale PID files

### `dev-tools cleanup-all`
Clean up all daemon processes

### `dev-tools restart <name>`
Restart a daemon process

### `dev-tools stop <name>`
Stop a daemon process

### `dev-tools version`
Show version information

### `dev-tools validate`
Validate configuration file

### `dev-tools completion <shell>`
Generate shell completion script


## Best Practices for AI Assistants

### Command Execution
- Always run dev-tools commands from the project root directory
- Use passthrough arguments with `--` separator: `dev-tools test -- --verbose`
- Check command output for errors before proceeding
- Use `dev-tools status` to check running daemons

### Flags
- `--verbose` or `-v`: Enable detailed logging
- `--project-dir <path>` or `-p <path>`: Run commands in a different directory
- `--no-color`: Disable colored output (useful for parsing)

### Debugging
- View recent logs: `dev-tools logs`
- Check daemon status: `dev-tools status`
- Clean up stale processes: `dev-tools cleanup-pids`

### Service Management

This project uses Docker services that are automatically managed:
- Services start automatically before commands that need them
- Health checks ensure services are ready before proceeding
- Use `dev-tools status` to see running services
- Check `activity.log` for service startup details


## Custom Configuration

This project has custom commands defined in `.dev-config.yaml`. These commands may:
- Start Docker containers or Docker Compose services
- Run multiple commands in sequence
- Execute commands in specific directories
- Run background daemons

Always check the configuration file for detailed command behavior.


## Common Workflows

### Running Tests
```bash
dev-tools test
```

### Starting Development Environment
```bash
dev-tools dev
```

### Checking Running Processes
```bash
dev-tools status
```

### Viewing Logs
```bash
dev-tools logs
```

## Notes

- This documentation was auto-generated by `dev-tools onboard`
- Commands and their behavior depend on project configuration
- Always verify command output before making assumptions about success
- Use `dev-tools --help` for more information

---

*Generated by dev-tools onboard command*
